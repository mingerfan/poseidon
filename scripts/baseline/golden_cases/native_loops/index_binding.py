@hc.func("c")
def golden(x):
    items=np.array([x*c0,x,x*2],dtype=object)
    for i in range(1,2):
        selected=items[i]
    for i in range(0):
        selected=items[i]
    return items[0]+items[i]+c1
